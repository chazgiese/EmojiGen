
var emojiBook = [ "👟", "🐇", "🍚", "📄", "🍚", "📄", "🍚", "📄", "🍚", "📄" ];
var upper = emojiBook.length;
var randomEmoji = Math.round(Math.random() * upper );

// OUTPUT: ("<div>" + randomEmoji +)

function randomEmoji( emoji ) {
  for (var i = 0; i < emoji.length; i += 1)
}

Math.ceil(Math.random() * upper)

emoji.length

function getEmoji() {
  Math.round(Math.random() * upper );
};

function randomEmoji() {
  var i = Math.round(Math.random() * emojiBook.length );
  var emoji = emojiBook[i];
  console.log(emoji)
}

randomEmoji()
